[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "{{PROJECT_NAME}}"
version = "0.1.0"
requires-python = ">=3.11"
description = "Projektgrundgerüst"

[tool.setuptools.packages.find]
where = ["src"]
