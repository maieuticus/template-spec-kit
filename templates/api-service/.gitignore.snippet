# Append these patterns to your project's .gitignore.
# (Editor swap files were missing in a real project's .gitignore baseline.)

# Editor / OS
*.swp
*.swo
*~

# Local configuration and operational data
.env
.env.*
!.env.example
!.env.test.example
backups/
